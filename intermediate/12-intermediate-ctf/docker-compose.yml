services:
  app:
    image: python:3.12-slim
    working_dir: /app
    command: ["python","app.py"]
    environment:
      WEB_FLAG_VALUE: ${WEB_FLAG_VALUE:-FLAG_NOT_CONFIGURED}
    ports:
      - "127.0.0.1:8440:8000"
    volumes:
      - ./app.py:/app/app.py:ro
