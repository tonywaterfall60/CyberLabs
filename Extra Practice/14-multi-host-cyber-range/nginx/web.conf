server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  add_header X-CyberLabs-Lab "multi-host-range" always;
  add_header X-CyberLabs-Service "web-frontend" always;
  location / { try_files $uri $uri/ =404; }
}