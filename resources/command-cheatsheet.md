# Command Cheatsheet

## Linux
```bash
pwd
ls -la
cd
cat
less
grep
find
head
tail
cp
mv
rm
mkdir
chmod
ps
ss
ip addr
ip route
```

## Networking
```bash
ping <host>
ip addr
ip route
ss -tulpn
curl http://<host>
```

## Git
```bash
git clone <url>
git status
git pull
git checkout -b <branch>
git add .
git commit -m "message"
git push
```
