services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.hardened.example
    ports:
      - "127.0.0.1:8590:5000"
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    environment:
      APP_MODE: training
