FROM python:3.13-slim

RUN useradd --create-home --uid 10001 appuser
WORKDIR /app
RUN printf 'hello\n' > app.txt && chown appuser:appuser app.txt

USER appuser
EXPOSE 5000
CMD ["python","-m","http.server","5000","--bind","0.0.0.0"]
