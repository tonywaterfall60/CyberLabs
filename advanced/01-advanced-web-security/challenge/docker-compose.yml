services:
  app:
    build: .
    environment:
      WEB_FLAG_VALUE: ${WEB_FLAG_VALUE:-FLAG_NOT_CONFIGURED}
      LOG_PATH: /logs/access.jsonl
    ports:
      - "127.0.0.1:8500:5000"
    volumes:
      - ./runtime:/logs
